<%= form_for([@task, @tcomment]) do |f| %>
  <% if tcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tcomment.errors.count, "error") %> prohibited this tcomment from being saved:</h2>

      <ul>
      <% tcomment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id, :value => @user_id, :readonly => true %>
  </div>

  <div class="field">
    <%= f.label :task_id %>
    <%= f.text_field :task_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
